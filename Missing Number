class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)         # len of nums.
        seen = set(nums)      #convert into set
        for i in range(n+1):
            if i not in seen:
                return i

        
