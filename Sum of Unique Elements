class Solution:
    def sumOfUnique(self, nums: List[int]) -> int:
        ans = 0
        for x in set(nums):
            if nums.count(x) == 1:
                ans += x    
        return ans        
